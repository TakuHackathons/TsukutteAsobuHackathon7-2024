/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // for Github Pages
  basePath: "/TsukutteAsobuHackathon7-2024",
  trailingSlash: true,
};

export default nextConfig;
